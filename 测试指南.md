# 测试指南

如何测试所有 Telegram 自动发布功能。

---

## 🧪 方法一：使用测试脚本（推荐）

### 1. 安装依赖（如果需要）

```bash
# 如果还没有安装 node-fetch
npm install node-fetch
```

### 2. 设置环境变量

在运行测试前，设置以下环境变量：

```bash
export CRON_SECRET="your_cron_secret_here"
export VERCEL_URL="your-project.vercel.app"  # 可选，如果不设置会使用 localhost
```

### 3. 运行测试

```bash
node test-telegram-features.js
```

---

## 🧪 方法二：使用 curl 命令

### 测试单个功能

```bash
# 设置变量
CRON_SECRET="your_cron_secret_here"
PROJECT_URL="your-project.vercel.app"

# 测试交易小贴士
curl -X GET "https://${PROJECT_URL}/api/cron/daily-trading-tip" \
  -H "Authorization: Bearer ${CRON_SECRET}" \
  -H "Content-Type: application/json"

# 测试新闻解读
curl -X GET "https://${PROJECT_URL}/api/cron/news-analysis" \
  -H "Authorization: Bearer ${CRON_SECRET}" \
  -H "Content-Type: application/json"

# 测试市场情绪卡片
curl -X GET "https://${PROJECT_URL}/api/cron/market-sentiment-card" \
  -H "Authorization: Bearer ${CRON_SECRET}" \
  -H "Content-Type: application/json"

# 测试市场故事
curl -X GET "https://${PROJECT_URL}/api/cron/daily-market-story" \
  -H "Authorization: Bearer ${CRON_SECRET}" \
  -H "Content-Type: application/json"

# 测试心理测试
curl -X GET "https://${PROJECT_URL}/api/cron/trading-psychology-quiz" \
  -H "Authorization: Bearer ${CRON_SECRET}" \
  -H "Content-Type: application/json"
```

---

## 🧪 方法三：在 Vercel Dashboard 中测试

### 1. 进入 Vercel Dashboard

1. 登录 https://vercel.com/
2. 选择你的项目
3. 进入 "Deployments" 标签

### 2. 查看函数日志

1. 点击最新的部署
2. 进入 "Functions" 标签
3. 可以看到所有 API 端点

### 3. 手动触发 Cron

1. 进入 "Settings" → "Cron Jobs"
2. 可以看到所有配置的 Cron 任务
3. 可以查看执行历史

---

## 🧪 方法四：使用 Vercel CLI

### 1. 安装 Vercel CLI

```bash
npm i -g vercel
```

### 2. 登录

```bash
vercel login
```

### 3. 链接项目

```bash
vercel link
```

### 4. 本地测试（需要本地开发环境）

```bash
vercel dev
```

然后在另一个终端运行测试脚本。

---

## ✅ 测试检查清单

### 功能测试

- [ ] 交易小贴士 - 检查 @http4477 是否收到消息
- [ ] 新闻解读 - 检查 @http4477 是否收到消息
- [ ] 市场情绪卡片 - 检查 @http4477 是否收到消息和图片
- [ ] 市场故事 - 检查 @http4477 是否收到消息
- [ ] 心理测试 - 检查 @http4477 是否收到消息
- [ ] 价格警报 - 检查 @http4477 是否收到警报
- [ ] 鲸鱼警报 - 检查 @http4477 是否收到警报

### 频道验证

- [ ] @http4477 收到所有非新闻内容
- [ ] @TranTradingLabNews 收到新闻内容
- [ ] 消息格式正确
- [ ] 链接和标签正常显示

### 错误检查

- [ ] 查看 Vercel 日志是否有错误
- [ ] 检查 API 调用是否成功
- [ ] 确认环境变量正确加载

---

## 🔍 常见问题排查

### 问题1: 401 Unauthorized

**原因**: CRON_SECRET 不正确

**解决**:
- 检查环境变量中的 CRON_SECRET
- 确认请求头中的 Authorization 格式正确

### 问题2: 500 Internal Server Error

**原因**: 可能是环境变量缺失或 API 调用失败

**解决**:
- 查看 Vercel 日志获取详细错误
- 确认所有必需的环境变量已配置
- 检查 OpenAI API 密钥是否有效

### 问题3: Telegram 消息没有发送

**原因**: Bot Token 或频道 ID 不正确

**解决**:
- 确认 TELEGRAM_BOT_TOKEN 正确
- 确认频道 ID 正确（@http4477 或 @TranTradingLabNews）
- 确认 Bot 已添加到频道并有发送权限

### 问题4: 内容生成失败

**原因**: OpenAI API 问题

**解决**:
- 检查 OpenAI API 密钥是否有效
- 确认 API 额度是否充足
- 查看日志中的具体错误信息

---

## 📊 预期结果

### 成功的响应示例

```json
{
  "success": true,
  "messageId": 12345,
  "timestamp": "2024-01-01T12:00:00.000Z"
}
```

### 失败的响应示例

```json
{
  "error": "Failed to generate content",
  "details": "OpenAI API error: ..."
}
```

---

## 💡 测试建议

1. **逐个测试**: 先测试一个功能，确认正常后再测试其他
2. **检查频道**: 每次测试后立即检查 Telegram 频道
3. **查看日志**: 在 Vercel Dashboard 查看详细日志
4. **等待时间**: API 调用可能需要几秒钟，请耐心等待

---

生成时间：${new Date().toLocaleString('zh-CN', { timeZone: 'Asia/Shanghai' })}
