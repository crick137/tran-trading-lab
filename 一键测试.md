# 一键测试指南

快速测试所有 Telegram 自动发布功能。

---

## 🚀 方法一：使用 curl 命令（最简单）

### Windows (PowerShell)

```powershell
# 设置变量
$PROJECT_URL = "your-project.vercel.app"
$CRON_SECRET = "your_cron_secret_here"

# 测试交易小贴士
curl -X GET "https://$PROJECT_URL/api/cron/daily-trading-tip" `
  -H "Authorization: Bearer $CRON_SECRET" `
  -H "Content-Type: application/json"

# 测试新闻解读
curl -X GET "https://$PROJECT_URL/api/cron/news-analysis" `
  -H "Authorization: Bearer $CRON_SECRET" `
  -H "Content-Type: application/json"

# 测试市场情绪卡片
curl -X GET "https://$PROJECT_URL/api/cron/market-sentiment-card" `
  -H "Authorization: Bearer $CRON_SECRET" `
  -H "Content-Type: application/json"
```

### Mac/Linux

```bash
# 设置变量
PROJECT_URL="your-project.vercel.app"
CRON_SECRET="your_cron_secret_here"

# 测试交易小贴士
curl -X GET "https://${PROJECT_URL}/api/cron/daily-trading-tip" \
  -H "Authorization: Bearer ${CRON_SECRET}" \
  -H "Content-Type: application/json"

# 测试新闻解读
curl -X GET "https://${PROJECT_URL}/api/cron/news-analysis" \
  -H "Authorization: Bearer ${CRON_SECRET}" \
  -H "Content-Type: application/json"

# 测试市场情绪卡片
curl -X GET "https://${PROJECT_URL}/api/cron/market-sentiment-card" \
  -H "Authorization: Bearer ${CRON_SECRET}" \
  -H "Content-Type: application/json"
```

---

## 🚀 方法二：使用测试脚本

### Windows

1. 编辑 `快速测试.bat`
2. 替换 `your-project.vercel.app` 和 `your_cron_secret_here`
3. 双击运行

### Mac/Linux

```bash
chmod +x 快速测试.sh
./快速测试.sh your-project.vercel.app your_cron_secret
```

---

## 🚀 方法三：在浏览器中测试（最简单）

### 1. 获取你的项目 URL

在 Vercel Dashboard 中找到你的项目 URL，例如：`tran-trading-lab.vercel.app`

### 2. 获取 CRON_SECRET

在 Vercel Dashboard → Settings → Environment Variables 中找到 `CRON_SECRET`

### 3. 使用以下 URL（替换变量）

```
https://your-project.vercel.app/api/cron/daily-trading-tip?secret=your_cron_secret
```

**注意**: 这种方法需要修改代码支持 query 参数，或者使用浏览器扩展添加 Authorization header。

---

## 🚀 方法四：使用 Postman 或 Insomnia

### 设置请求

1. **URL**: `https://your-project.vercel.app/api/cron/daily-trading-tip`
2. **Method**: `GET`
3. **Headers**:
   - `Authorization`: `Bearer your_cron_secret_here`
   - `Content-Type`: `application/json`

### 测试的功能列表

- `/api/cron/daily-trading-tip` - 交易小贴士
- `/api/cron/news-analysis` - 新闻解读
- `/api/cron/market-sentiment-card` - 市场情绪卡片
- `/api/cron/daily-market-story` - 市场故事
- `/api/cron/trading-psychology-quiz` - 心理测试
- `/api/cron/price-alerts` - 价格警报
- `/api/cron/whale-alerts` - 鲸鱼警报

---

## ✅ 预期结果

### 成功响应

```json
{
  "success": true,
  "messageId": 12345,
  "timestamp": "2024-01-01T12:00:00.000Z"
}
```

### 检查 Telegram

测试后，立即检查：
- **@http4477** 频道是否收到消息
- 消息内容是否正确
- 格式是否美观

---

## 🔍 如果测试失败

### 1. 检查环境变量

确认以下变量已配置：
- `TELEGRAM_BOT_TOKEN`
- `TELEGRAM_MAIN_CHANNEL_ID` = `@http4477`
- `OPENAI_API_KEY`
- `CRON_SECRET`

### 2. 查看 Vercel 日志

1. 进入 Vercel Dashboard
2. 选择项目 → Deployments
3. 点击最新部署 → Functions
4. 查看执行日志

### 3. 常见错误

- **401 Unauthorized**: CRON_SECRET 不正确
- **500 Internal Server Error**: 查看日志获取详细错误
- **消息未发送**: 检查 Bot Token 和频道权限

---

## 💡 快速测试命令（复制粘贴）

### 测试交易小贴士

```bash
curl -X GET "https://your-project.vercel.app/api/cron/daily-trading-tip" \
  -H "Authorization: Bearer your_cron_secret" \
  -H "Content-Type: application/json"
```

### 测试新闻解读

```bash
curl -X GET "https://your-project.vercel.app/api/cron/news-analysis" \
  -H "Authorization: Bearer your_cron_secret" \
  -H "Content-Type: application/json"
```

### 测试市场情绪卡片

```bash
curl -X GET "https://your-project.vercel.app/api/cron/market-sentiment-card" \
  -H "Authorization: Bearer your_cron_secret" \
  -H "Content-Type: application/json"
```

---

**替换 `your-project.vercel.app` 和 `your_cron_secret` 后直接运行！**
