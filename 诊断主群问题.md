# 主群 (@http4477) 问题诊断

## 问题描述
新闻功能正常（发送到 @TranTradingLabNews），但主群 (@http4477) 没有收到任何消息。

## 可能原因

### 1. 环境变量未设置
在 Vercel Dashboard 中需要设置：
- `TELEGRAM_MAIN_CHANNEL_ID` = `@http4477` 或频道的数字ID

### 2. Bot 权限问题
Bot 需要是 `@http4477` 频道的管理员，并且有"发送消息"权限。

### 3. 频道ID格式问题
如果频道是私有的，需要使用数字ID而不是 `@http4477`。

## 解决步骤

### 步骤1：检查环境变量
1. 登录 Vercel Dashboard
2. 进入项目设置 → Environment Variables
3. 确认以下变量已设置：
   - `TELEGRAM_BOT_TOKEN` ✅
   - `TELEGRAM_MAIN_CHANNEL_ID` = `@http4477` 或数字ID
   - `OPENAI_API_KEY` ✅
   - `GEMINI_API_KEY` (用于生成图片的功能)
   - `GROQ_API_KEY` (如果使用)

### 步骤2：检查Bot权限
1. 打开 Telegram，进入 `@http4477` 频道
2. 点击频道名称 → 编辑
3. 管理员 → 添加管理员
4. 搜索你的 Bot 用户名并添加
5. 确保勾选"发送消息"权限

### 步骤3：获取频道数字ID（如果使用用户名不工作）
1. 将 Bot 添加为频道管理员
2. 在频道中发送一条测试消息
3. 访问：`https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
4. 在返回的 JSON 中找到 `chat.id`（通常是负数）
5. 在 Vercel 中设置 `TELEGRAM_MAIN_CHANNEL_ID` = 这个数字ID

### 步骤4：测试单个功能
使用以下命令测试：

```powershell
# 测试晚间总结
$url = "https://tran-trading-ep4c7daw5-crick137s-projects.vercel.app/api/cron/evening-summary"
$secret = "gwreeeee98234r7guvh23nori88v43999999tnio3o5iii9v"
$headers = @{"Authorization" = "Bearer $secret"}
Invoke-RestMethod -Uri $url -Method Get -Headers $headers
```

## 检查清单

- [ ] `TELEGRAM_MAIN_CHANNEL_ID` 已在 Vercel 中设置
- [ ] Bot 是 `@http4477` 频道的管理员
- [ ] Bot 有"发送消息"权限
- [ ] 如果使用用户名不工作，尝试使用数字ID
- [ ] 检查 Vercel 部署日志中的错误信息
