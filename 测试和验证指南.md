# 测试和验证指南

环境变量已配置完成！现在让我们验证所有功能是否正常工作。

---

## ✅ 验证步骤

### 1. 检查环境变量是否生效

在 Vercel Dashboard 中：
1. 进入项目 → Settings → Environment Variables
2. 确认以下变量都已存在：
   - ✅ TELEGRAM_BOT_TOKEN
   - ✅ TELEGRAM_CHANNEL_ID
   - ✅ OPENAI_API_KEY
   - ✅ CRON_SECRET
   - ✅ KV_REST_API_URL
   - ✅ KV_REST_API_TOKEN
   - ✅ GEMINI_API_KEY (可选)
   - ✅ GROQ_API_KEY (可选)

### 2. 检查 Cron 任务配置

在 `vercel.json` 中确认所有 Cron 任务已配置：

```json
{
  "crons": [
    { "path": "/api/cron/publish-news", "schedule": "0 * * * *" },
    { "path": "/api/cron/morning-briefing", "schedule": "0 23 * * *" },
    { "path": "/api/cron/evening-summary", "schedule": "0 11 * * *" },
    { "path": "/api/cron/price-alerts", "schedule": "*/5 * * * *" },
    { "path": "/api/cron/whale-alerts", "schedule": "*/10 * * * *" },
    { "path": "/api/cron/weekly-report", "schedule": "0 1 * * 0" },
    { "path": "/api/cron/economic-calendar", "schedule": "30 22 * * *" },
    { "path": "/api/cron/technical-analysis", "schedule": "0 3 * * *" }
  ]
}
```

### 3. 手动测试 API 端点

#### 测试新闻发布（需要 CRON_SECRET）

```bash
curl -X GET "https://your-project.vercel.app/api/cron/publish-news" \
  -H "Authorization: Bearer YOUR_CRON_SECRET"
```

#### 测试健康检查（不需要认证）

```bash
curl https://your-project.vercel.app/api/health
```

### 4. 检查部署日志

1. 进入 Vercel Dashboard → Deployments
2. 查看最新的部署日志
3. 检查是否有错误信息
4. 特别关注：
   - 环境变量加载是否成功
   - API 调用是否正常
   - Telegram 消息是否发送成功

### 5. 验证 Telegram Bot

1. 在 Telegram 中搜索你的 Bot（通过 Bot Token 可以找到用户名）
2. 确认 Bot 已添加到频道并具有发送消息权限
3. 手动触发一次测试，查看频道是否收到消息

---

## 🧪 测试各个功能

### 测试早晨简报

等待或手动触发：
- 时间：每天 UTC 23:00（KST 08:00）
- 检查频道是否收到完整的市场简报

### 测试新闻发布

等待或手动触发：
- 时间：每小时整点
- 检查频道是否收到 Top 5 新闻

### 测试价格警报

等待或手动触发：
- 时间：每 5 分钟
- 当价格突破关键水平时，应该收到警报

### 测试鲸鱼警报

等待或手动触发：
- 时间：每 10 分钟
- 当检测到大额交易时，应该收到警报

---

## 📊 监控建议

### 1. 设置 Vercel 日志监控

- 在 Vercel Dashboard 中启用日志
- 设置错误警报（如果有）

### 2. 监控 API 使用量

- **OpenAI API**：在 https://platform.openai.com/usage 查看使用量
- **Telegram Bot**：检查消息发送是否成功
- **Vercel KV**：在 Vercel Dashboard 查看存储使用情况

### 3. 检查 Cron 执行情况

- 在 Vercel Dashboard → Deployments → Functions
- 查看 Cron 任务的执行历史
- 确认任务按时执行

---

## ⚠️ 常见问题排查

### 问题 1：Cron 任务没有执行

**检查**：
- `vercel.json` 中的 cron 配置是否正确
- 项目是否已部署到 Production
- Vercel 账户是否有 Cron 功能权限

### 问题 2：Telegram 消息没有发送

**检查**：
- Bot Token 是否正确
- Bot 是否已添加到频道并具有发送权限
- 频道 ID 是否正确（数字 ID 或用户名）

### 问题 3：AI 生成失败

**检查**：
- OPENAI_API_KEY 是否有效
- API 密钥是否有足够的额度
- 查看 Vercel 日志中的错误信息

### 问题 4：状态存储不工作

**检查**：
- KV_REST_API_URL 和 KV_REST_API_TOKEN 是否正确
- Vercel KV 数据库是否已创建
- 查看日志中的 KV 连接错误

---

## 🎯 下一步

1. ✅ **等待第一次 Cron 执行**
   - 观察日志，确认任务正常执行
   - 检查 Telegram 频道是否收到消息

2. ✅ **监控运行情况**
   - 持续观察 24-48 小时
   - 确认所有定时任务都正常工作

3. ✅ **优化配置**（可选）
   - 根据实际使用情况调整 Cron 频率
   - 优化 AI 提示词以获得更好的内容
   - 调整价格警报的关键水平

---

## 📝 维护清单

### 每日检查
- [ ] 确认早晨简报已发送
- [ ] 确认晚间总结已发送
- [ ] 检查错误日志

### 每周检查
- [ ] 确认周报已发送
- [ ] 检查 API 使用量和费用
- [ ] 检查 KV 存储使用情况

### 每月检查
- [ ] 轮换 API 密钥（安全最佳实践）
- [ ] 检查并更新依赖包
- [ ] 审查和优化代码

---

## 🆘 需要帮助？

如果遇到问题：

1. **查看 Vercel 日志**：Dashboard → Deployments → 选择部署 → Functions
2. **检查环境变量**：确认所有变量都已正确配置
3. **测试单个功能**：手动触发 API 端点进行测试
4. **查看错误信息**：日志中通常有详细的错误描述

---

**配置完成！** 🎉

现在你的自动发布系统应该可以正常工作了。建议先观察几天，确保所有功能都按预期运行。

生成时间：${new Date().toLocaleString('zh-CN', { timeZone: 'Asia/Shanghai' })}
