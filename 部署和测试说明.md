# 部署和测试说明

## ⚠️ 重要：需要重新部署

你添加了新的 API 端点，**必须重新部署项目**才能使用！

---

## 🚀 重新部署步骤

### 方法一：通过 Git 推送（推荐）

```bash
git add .
git commit -m "添加新的Telegram自动发布功能"
git push
```

Vercel 会自动检测到推送并重新部署。

### 方法二：在 Vercel Dashboard 中重新部署

1. 登录 https://vercel.com/
2. 选择你的项目
3. 进入 "Deployments" 标签
4. 点击最新部署右侧的 "..." 菜单
5. 选择 "Redeploy"

---

## 🧪 部署后测试

### 使用 PowerShell

```powershell
$PROJECT_URL = "tran-trading-ep4c7daw5-crick137s-projects.vercel.app"
$CRON_SECRET = "gwreeeee98234r7guvh23nori88v43999999tnio3o5iii9v"

# 测试交易小贴士
curl -X GET "https://$PROJECT_URL/api/cron/daily-trading-tip" `
  -H "Authorization: Bearer $CRON_SECRET" `
  -H "Content-Type: application/json"
```

### 或者使用脚本

```powershell
.\快速测试.ps1 tran-trading-ep4c7daw5-crick137s-projects.vercel.app
```

---

## ✅ 测试检查清单

部署完成后，测试以下内容：

### 1. 发送功能 ✅
- [ ] API 返回 `success: true`
- [ ] 返回了 `messageId`
- [ ] 状态码是 200

### 2. AI调用 ✅
- [ ] 响应时间 > 3秒（说明AI在处理）
- [ ] 没有返回错误信息

### 3. 内容质量 ✅
- [ ] Telegram 频道 @http4477 收到消息
- [ ] 消息格式正确
- [ ] 内容专业且有用
- [ ] 包含具体数据和数字
- [ ] 语言流畅自然

---

## 🔍 如果测试失败

### 404 错误（页面未找到）

**原因**: 新功能未部署

**解决**:
1. 确认已重新部署项目
2. 等待部署完成（通常1-2分钟）
3. 再次运行测试

### 401 错误（未授权）

**原因**: CRON_SECRET 不正确

**解决**:
- 检查环境变量中的 CRON_SECRET
- 确认请求头格式正确

### 500 错误（服务器错误）

**原因**: 可能是环境变量缺失或API调用失败

**解决**:
- 查看 Vercel 日志获取详细错误
- 确认所有环境变量已配置
- 检查 OpenAI API 密钥是否有效

---

## 📝 测试命令（完整版）

```powershell
# 设置变量
$PROJECT_URL = "tran-trading-ep4c7daw5-crick137s-projects.vercel.app"
$CRON_SECRET = "gwreeeee98234r7guvh23nori88v43999999tnio3o5iii9v"

# 测试1: 交易小贴士
Write-Host "`n测试1: 每日交易小贴士" -ForegroundColor Yellow
curl -X GET "https://$PROJECT_URL/api/cron/daily-trading-tip" `
  -H "Authorization: Bearer $CRON_SECRET" `
  -H "Content-Type: application/json"

Start-Sleep -Seconds 3

# 测试2: 新闻解读
Write-Host "`n测试2: 新闻深度解读" -ForegroundColor Yellow
curl -X GET "https://$PROJECT_URL/api/cron/news-analysis" `
  -H "Authorization: Bearer $CRON_SECRET" `
  -H "Content-Type: application/json"

Start-Sleep -Seconds 3

# 测试3: 市场情绪卡片
Write-Host "`n测试3: 市场情绪卡片" -ForegroundColor Yellow
curl -X GET "https://$PROJECT_URL/api/cron/market-sentiment-card" `
  -H "Authorization: Bearer $CRON_SECRET" `
  -H "Content-Type: application/json"

Write-Host "`n测试完成! 请检查 Telegram @http4477 频道" -ForegroundColor Green
```

---

## 💡 下一步

1. **重新部署项目**（必须！）
2. **等待部署完成**（1-2分钟）
3. **运行测试命令**
4. **检查 Telegram 频道**

---

**重要**: 新功能必须重新部署后才能使用！
